<Window x:Class="AppGamesTito.recuperarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppGamesTito"
        mc:Ignorable="d"
         Title="Recuperação" Height="400" Width="400">
    <Grid Margin="20"
          >
        <!--Definição das linhas-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--Text Usuario-->
        <TextBlock Grid.Row="0"
                   Text="Usuario|Email"/>
        <TextBox Grid.Row="1"
                 x:Name="txtAlterar"
                 Height="25"
                 Margin="0,5,0,10"/>
        <Button Grid.Row="2"
                Content="Enviar"
                Height="25"
                BorderThickness="0"
                Margin="0,10,0,5"
                x:Name="btnEnviar" Click="btnEnviar_Click"/>

    </Grid>
</Window>
